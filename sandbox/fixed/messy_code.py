def is_within_range(z):
    """
    Checks if a given number z is within the range (0, 100).
    
    Args:
        z (int): The number to check.
    
    Returns:
        bool: True if z is within the range, False otherwise.
    """
    return 0 < z < 100